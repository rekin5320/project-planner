version: "3.8"

services:
    mysql:
        image: mysql:8.2.0-oracle
        container_name: pap-mysql
        environment:
            MYSQL_DATABASE: papdb
            MYSQL_USER: myuser
            MYSQL_PASSWORD: "2L9(4Evz,9"
            MYSQL_ROOT_PASSWORD: "E{E28>kU_i"
        ports:
            - "30306:3306"
        volumes:
            - ./mysql-data:/var/lib/mysql
