<persistence xmlns="http://java.sun.com/xml/ns/persistence"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation=
        "http://java.sun.com/xml/ns/persistence
        http://java.sun.com/xml/ns/persistence/persistence_2_0.xsd"
        version="2.0">
    <persistence-unit name="PW.PAP" transaction-type="RESOURCE_LOCAL">
        <!-- Persistence provider -->
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>
        <!-- Entity classes -->
        <class>pw.pap.User</class>

        <properties>
            <!-- The JDBC URL to the database instance -->
            <property name="jakarta.persistence.jdbc.url"
                value="jdbc:mysql://rekin.ddns.net:30306/papdb" />
            <!-- The database username -->
            <property name="jakarta.persistence.jdbc.user" value="myuser" />
            <!-- The database password -->
        <property name="jakarta.persistence.jdbc.password" value="2L9(4Evz,9"/>

            <property name="hibernate.hbm2ddl.auto" value="update" />
        </properties>
    </persistence-unit>
</persistence>